spring:
  application:
    name: dispatch

  datasource:
    url: jdbc:postgresql://localhost:5432/ambulancedb
    username: postgres
    password: dibagar66
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
